{
  "name": "ifs-mcp-server",
  "version": "1.1.0",
  "description": "MCP server for authenticated IFS Cloud API access via OAuth 2.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc && node -e \"const fs=require('fs');const p=require('path');const src='src/resources';const dst='build/resources';fs.readdirSync(src).filter(f=>f.endsWith('.md')).forEach(f=>fs.copyFileSync(p.join(src,f),p.join(dst,f)))\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ifs": "./build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.2",
    "axios": "^1.13.1",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^24.9.2",
    "typescript": "^5.9.3"
  },
  "files": [
    "build"
  ]
}
